const addTaskToList = valueInput => {
    console.log(`add a task ${valueInput}`)
};
const updateDatabase = valueInput => {console.log(`atualizou o db com ${valueInput}`)};

export {addTaskToList, updateDatabase};